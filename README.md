# Geometric Functional Inference in 3D Scenes

This repository contains the experimental code developed for the Master's dissertation.

## Structure

- `src/` – core geometry and inference logic  
- `scripts/` – experiment runners (batch, sweep, evaluation)  
- `configs/` – experimental parameters  

## Requirements

Python 3.11

Install dependencies:

```bash
pip install -r requirements.txt
